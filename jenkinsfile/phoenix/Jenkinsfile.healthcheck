podTemplate(containers: [
    containerTemplate(name: 'golang', image: 'hello-world', ttyEnabled: true, command: 'cat')
  ]) {

    node(POD_LABEL) {
        stage('Get a Golang project') {
            container('golang') {
                stage('Build a Go project') {
                    echo "test"
                }
            }
        }

    }
}