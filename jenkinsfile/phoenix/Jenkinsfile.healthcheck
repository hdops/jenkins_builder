podTemplate(containers: [containerTemplate(name: 'kaniko', image: 'nginx', command: 'sleep', args: '10')]) {
    node(POD_LABEL) {
        container(name: 'kaniko') {
            sh 'echo $PATH'
        }
    }
}