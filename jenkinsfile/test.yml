def worker = 'master'
if (env.worker){
    worker= env.worker
}

pipeline{
    agent {label worker}
    stages{
        stage('test'){
            steps{
                script{
                    echo "hello world"   
                }
                
            }
        }
    }
}
