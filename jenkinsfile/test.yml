def worker = 'master'
if (env.worker){
    worker= env.worker
}
def git_repo="test_git"
pipeline{
    agent {label worker}
    stages{
        stage('test'){
            steps{
                script{
                    echo "hello world"
                    checkout([$class: 'GitSCM', branches: [[name: '*/master']],
						userRemoteConfigs: [[
							url: "https://gitee.com/hdops/${git_repo}}.git"]]])
                }
                
            }
        }
    }
}
